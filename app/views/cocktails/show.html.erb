<h2><%= @cocktail.name %></h2>
<p>Composition :</p>
<ul>
  <% @doses.each do |dose| %>
    <li><%= "{dose.name} : {dose.description}" %></li>
  <% end %>
</ul>

<%= link_to "ajouter un ingrédient", new_cocktail_dose_path(@cocktail) %>
